# `lua/config/autocmds.lua`

## 作用

这个文件当前没有增加任何自定义自动命令。

## 为什么这样写

- 先保留空文件，表示“这里是未来放自定义 autocmd 的固定入口”。
- 不急着往这里堆行为，可以避免编辑器启动后出现难追踪的隐式状态。
- LazyVim 自带了大量默认 autocmd，这里不重写，说明当前配置更偏向“先改键位和导航，再补自动化行为”。

## 当前策略

- 只有在某个行为确实适合事件驱动时，才放进这里。
- 如果能通过插件选项解决，就尽量不额外加 autocmd。

